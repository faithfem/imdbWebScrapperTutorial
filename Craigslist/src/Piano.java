public class Piano {

    String searchQuery = "Piano";
    WebClient client = new WebClient();
    client.getOptions().setCssEnabled(false);
    client.getOptions().setJavaScriptEnabled(false);

    try{
        String searchUrl = "https://providence.craigslist.org/search/sss?query=piano&sort=rel"

    }
}
